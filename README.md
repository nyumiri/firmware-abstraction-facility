# Firmware Abstraction Facility (FAF)

This library provides an abstraction layer between hardware and application code, helping you write high-performance, decoupled firmware — so you don't have to rewrite the same logic for every different microcontroller.

## Architecture

The architecture is based on the **Hardware Abstraction Layer** (HAL) model, where users can create drivers for specific components following the library's expected interface, and use them in the application through the API each driver exposes.

```mermaid
graph TB
    subgraph ESP32[Target: ESP32 / ESP-IDF]
        direction TB
        Main1[Main Application<br>External Entrypoint]:::external -.-> Common1[[Firmware Abstraction Facility<br/>Common API]]
        Common1 === Driver1{{FAF Driver Internal}}
        Common1 === OS1{{FAF Sync API}}
        Driver1 -->|Vendor Library API| Vendor1(Vendor Layer<br/>ESP32-IDF)
        OS1 -->|"RTOS Thread API<br>Mutex / Semaphore"| RTOS1(RTOS Layer<br/>FreeRTOS)
        Vendor1 -.-> HW1([Hardware ESP32])
        RTOS1 -.-> HW1
    end

    subgraph Generic[Target: Generic MCU]
        direction TB
        Main2[Main Application<br>External Entrypoint]:::external -.-> Common2[[Firmware Abstraction Facility<br/>Common API]]
        Common2 === Driver2{{FAF Driver Internal}}
        Common2 === OS2{{FAF Sync API}}
        Driver2 -->|Vendor Library API| Vendor2(Vendor Layer<br/>STM32Cube HAL)
        OS2 -->|"RTOS Thread API<br>Mutex / Semaphore"| RTOS2(RTOS Layer<br/>Zephyr RTOS)
        Vendor2 -.-> HW2([Hardware STM32])
        RTOS2 -.-> HW2
    end

    subgraph Arduino[Target: Arduino]
        direction TB
        Main3[Main Application<br>External Entrypoint]:::external -.-> Common3[[Firmware Abstraction Facility<br/>Common API]]
        Common3 === Driver3{{FAF Driver Internal}}
        Common3 === OS3{{FAF Sync API}}
        Driver3 -->|Vendor Library API| Vendor3(Vendor Layer<br/>Arduino SDK)
        OS3 -->|Hardware Interrupt API| Bare3(Bare-metal Sync<br/>interrupt masking)
        Vendor3 -.-> HW3([Hardware Arduino])
        Bare3 -.-> HW3
    end

    ESP32~~~Generic
    Generic~~~Arduino

    classDef external fill:none,stroke:#888,stroke-width:1px,stroke-dasharray: 4 4,color:#888
```

Drivers are made available through a global list generated by a built-in **Domain-Specific Language** that resolves entirely at compile time, with no need for external scripts or equivalent tooling.

## Roadmap

### Alpha v0.0.1:
- [x] Basic module structure.
- [x] DSL for declarative configuration.
- [x] Core-lib unit tests on Desktop environment.
- [x] Usage examples for v0.0.1a.

### Current Plains:
- [x] Generic Calls Implemented - Drivers Refactored on New Dispatch System.
- [x] Total Refactor on Library Core to Fix Issues and Bottlenecks.
- [ ] Thread-Safety for full RTOS compatibility.
- [ ] Basic drivers for the native ESP32-IDF platform.

> **Status:** Stable Version in development - Branch `Dev` Active

## License
The released code is licensed under the MIT license.